import { translateInstance } from '../translatable_text/instance';
import { HEADER_PART } from '../translations_parts/translations_parts';

export const translatableText = translateInstance(HEADER_PART);