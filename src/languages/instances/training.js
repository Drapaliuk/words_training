import { translateInstance } from '../translatable_text/instance';
import { TRAINING_PART } from '../translations_parts/translations_parts';

export const translatableText = translateInstance(TRAINING_PART);