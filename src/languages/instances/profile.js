import { translateInstance } from '../translatable_text/instance';
import { PROFILE_PART } from '../translations_parts/translations_parts';

export const translatableText = translateInstance(PROFILE_PART);