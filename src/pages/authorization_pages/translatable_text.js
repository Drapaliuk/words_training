import  {translateInstance}  from '../../languages/translatable_text/instance';
import { AUTH_PART } from '../../languages/translations_parts/translations_parts';

export const translatableText = translateInstance(AUTH_PART)