import  {translateInstance}  from '../../languages/translatable_text/instance';
import { TRAINING_PART } from '../../languages/translations_parts/translations_parts';

export const translatableText = translateInstance(TRAINING_PART)